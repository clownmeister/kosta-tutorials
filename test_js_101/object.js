//(let username) = deklarace ;('lucian') = asign(přiřazená hodnota)
let username = 'lucian';
write(username)

username = 'kosta';
write(username)
//rovnou deklaruješ s asignem
const gravity = 9.81;
write(gravity)

const user = {
    // Jak se nazývají tyto položky jako firstName???????????????????? Neni to let?
    firstName: 'lucian',
    lastName: 'blazek',
}
write(user.firstName + ' ' + user.lastName)

renderUser(user)



